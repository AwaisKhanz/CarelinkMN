[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm --filter @carelink/database db:generate", "pnpm build"]

[start]
cmd = "PORT=3001 node packages/api/dist/index.js & (cd apps/web && PORT=3000 node_modules/.bin/next start)"

[variables]
NODE_ENV = "production"
